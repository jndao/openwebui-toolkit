# Test dependencies for context_manager v0.1.0-preview.1
# Install with: uv pip install -r requirements-test.txt

# Testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Required for real token counting (not mocked)
tiktoken>=0.5.0

# Required for Valve validation
pydantic>=2.0.0

# Required for Request type hints
fastapi>=0.100.0
